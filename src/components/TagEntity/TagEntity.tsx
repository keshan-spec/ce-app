const TagEntity: React.FC = () => {
    return (
        <div className="flex items-center justify-center w-10 h-10 bg-primary text-white rounded-full">
            <span className="text-sm font-bold">A</span>
        </div>
    );
};

export default TagEntity;